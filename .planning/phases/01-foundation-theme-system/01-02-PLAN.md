---
wave: 2
depends_on: [01-01]
requirements_addressed: [THEM-01, THEM-02, THEM-03, UXIN-01]
files_modified: [src/theme/ThemeContext.tsx, src/theme/themeConfig.ts, src/theme/index.ts, src/components/Preloader/Preloader.tsx, src/components/Preloader/Preloader.css, src/components/Preloader/index.ts, src/App.tsx, src/index.css]
autonomous: true
---

# Plan: ThemeProvider, CSS Design Tokens, and Preloader

## Goal
Implement the central ThemeProvider, map design tokens to CSS Custom Properties, and build the premium Preloader component.

## Tasks

<task id="01-02-01">
<action>
Create src/theme/themeConfig.ts holding the luxury palette (blue #020B1E, white #FFFFFF, gold #D4AF37, silver #D8D9DA), typography config, spacing, and transitions.
</action>
<read_first>
src/theme/themeConfig.ts
</read_first>
<acceptance_criteria>
src/theme/themeConfig.ts exists and exports a configuration object containing colors, typography, spacing, and transition constants.
</acceptance_criteria>
</task>

<task id="01-02-02">
<action>
Implement the src/theme/ThemeContext.tsx ThemeProvider. It must inject all themeConfig design tokens into the :root element as CSS Custom Properties (--color-background, --color-accent, --font-heading, etc.) on mount and export a useTheme hook.
</action>
<read_first>
src/theme/ThemeContext.tsx
</read_first>
<acceptance_criteria>
src/theme/ThemeContext.test.tsx Vitest suite compiles and verifies the custom useTheme hook returns active theme configuration.
</acceptance_criteria>
</task>

<task id="01-02-03">
<action>
Implement the folder-per-component src/components/Preloader (Preloader.tsx, Preloader.css, index.ts). Preloader should render a full-screen entry overlay with a gold glowing brand mark/logo and fade out after a brief interval.
</action>
<read_first>
src/components/Preloader/Preloader.tsx
</read_first>
<acceptance_criteria>
src/components/Preloader/Preloader.test.tsx Vitest suite passes, confirming loader rendering and transition states.
</acceptance_criteria>
</task>

<task id="01-02-04">
<action>
Update src/App.tsx to wrap the application in ThemeProvider, render the Preloader, and verify global styles correctly load colors/fonts from root variables.
</action>
<read_first>
src/App.tsx
</read_first>
<acceptance_criteria>
The dev server builds without error and the main viewport displays body background colored with var(--color-background).
</acceptance_criteria>
</task>

## Must Haves
- Theme values dynamically serializing to CSS custom properties.
- Preloader component behaving correctly on initial paint.
