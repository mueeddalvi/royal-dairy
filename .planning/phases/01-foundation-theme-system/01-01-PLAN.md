---
wave: 1
depends_on: []
requirements_addressed: [ARCH-01, ARCH-02, ARCH-03]
files_modified: [package.json, tsconfig.json, vite.config.ts, src/main.tsx, src/index.css, src/theme/ThemeContext.test.tsx, src/components/Preloader/Preloader.test.tsx]
autonomous: true
---

# Plan: Vite & Architecture Setup

## Goal
Scaffold the React + TypeScript + Vite workspace and construct the folder-per-component hierarchy.

## Tasks

<task id="01-01-01">
<action>
Initialize the Vite React TS template and install development dependencies (Vitest, @testing-library/react, @testing-library/jest-dom, jsdom).
</action>
<read_first>
package.json
</read_first>
<acceptance_criteria>
package.json exists and contains react, typescript, vite, vitest, and @testing-library/react.
</acceptance_criteria>
</task>

<task id="01-01-02">
<action>
Create base tsconfig.json and vite.config.ts to support Vitest environment setup.
</action>
<read_first>
vite.config.ts
</read_first>
<acceptance_criteria>
vite.config.ts exists and includes a reference to "test" configuration using "jsdom" environment.
</acceptance_criteria>
</task>

<task id="01-01-03">
<action>
Establish directory structure (src/components, src/sections, src/layouts, src/theme) and configure basic app mounting in src/main.tsx and src/index.css.
</action>
<read_first>
src/main.tsx
</read_first>
<acceptance_criteria>
src/main.tsx exists, compiles, and successfully references global index.css reset styles.
</acceptance_criteria>
</task>

<task id="01-01-04">
<action>
Set up test stub files (src/theme/ThemeContext.test.tsx, src/components/Preloader/Preloader.test.tsx) to verify Vitest configuration runs cleanly.
</action>
<read_first>
src/theme/ThemeContext.test.tsx
</read_first>
<acceptance_criteria>
Running "npm run test --run" completes and succeeds (even with basic placeholder assertions).
</acceptance_criteria>
</task>

## Must Haves
- Compiling React + TS + Vite framework.
- Vitest testing framework active with DOM test mocks.
