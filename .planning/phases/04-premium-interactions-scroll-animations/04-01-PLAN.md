---
wave: 1
depends_on: []
requirements_addressed: [UXIN-02, UXIN-03]
files_modified: [src/hooks/useScrollReveal.ts, src/hooks/useScrollReveal.test.ts, src/components/Header/Header.tsx, src/components/Header/Header.css, src/components/Header/index.ts, src/components/Header/Header.test.tsx, src/App.tsx, src/index.css]
autonomous: true
---

# Plan: Scroll Reveal Hook & Glassmorphic Navigation Header

## Goal
Implement viewport-based entrance animations via custom hook, and design the sticky glassmorphic header navigation containing anchor jump highlights.

## Tasks

<task id="04-01-01">
<action>
Create src/hooks/useScrollReveal.ts implementing custom React hook using IntersectionObserver to assign visible classes to elements upon entering viewport.
</action>
<read_first>
src/hooks/useScrollReveal.ts
</read_first>
<acceptance_criteria>
src/hooks/useScrollReveal.test.ts passes, verifying observer creation and class assignments.
</acceptance_criteria>
</task>

<task id="04-01-02">
<action>
Add reveal transition classes inside src/index.css (e.g. .reveal-up opacity/translate rules, and .reveal-up.is-visible transitions). Integrate this hook into landing page sections.
</action>
<read_first>
src/index.css
</read_first>
<acceptance_criteria>
The project builds cleanly and transition styles are configured correctly.
</acceptance_criteria>
</task>

<task id="04-01-03">
<action>
Implement folder-per-component src/components/Header (Header.tsx, Header.css, index.ts, Header.test.tsx). Support sticky alignment, gold logo branding, navigation links, and a responsive mobile toggle drawer menu.
</action>
<read_first>
src/components/Header/Header.tsx
</read_first>
<acceptance_criteria>
src/components/Header/Header.test.tsx passes, confirming active sections link presence.
</acceptance_criteria>
</task>

## Must Haves
- useScrollReveal hook observing targets.
- Hardware-accelerated CSS transition keyframes (fade up).
- Sticky glassmorphic header overlaying content with backdrop blur support.
- Native smooth scrolling on anchor click jumps.
